apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - deployment.yaml
  - service.yaml   # if any
  - namespace.yaml # if using a custom namespace (optional)
